var csv = [];var totalSales = 0;var avgSale = 0;var numTransactions = 0;var csvFile = 'sales.csv';var table = document.createElement('table');document.body.appendChild(table);fetch(csvFile).then(response => response.text()).then(data => {csv = data.split('\n');csv.forEach(row => {var columns = row.split(',');totalSales += parseFloat(columns[1]);numTransactions++;if (columns.length > 2) {avgSale = totalSales / numTransactions;}});var row = table.insertRow(-1);var cell1 = row.insertCell(0);cell1.innerHTML = 'Total Sales: $' + totalSales.toFixed(2);var cell2 = row.insertCell(1);cell2.innerHTML = 'Average Sale: $' + avgSale.toFixed(2);var cell3 = row.insertCell(2);cell3.innerHTML = 'Number of Transactions: ' + numTransactions;});