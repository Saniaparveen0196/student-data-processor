const url = window.location.search.replace('?url=', '');const image = document.getElementById('image');image.src = url || 'sample.jpg';const ctx = document.getElementById('chart').getContext('2d');fetch('products.json').then(response => response.json()).then(data => {const labels = data.map(item => item.name);const datasets = [{label: 'Product Sales',data: data.map(item => item.sales)}];const myChart = new Chart(ctx, {type: 'bar',data: {labels,datasets},options: {title: {display: true,text: 'Product Sales'}}});});